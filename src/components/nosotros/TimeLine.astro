---
const timeline = [
  { year: "1989", title: "Los inicios", desc: "Nace el equipo núcleo con foco en servicios contables y asesoría a pymes." },
  { year: "2005", title: "Expansión", desc: "Integramos ingeniería y mejora de procesos, sumando capacitación empresarial." },
  { year: "2016", title: "Transformación digital", desc: "ERP/CRM, trazabilidad y ciberseguridad para sectores regulados." },
  { year: "Hoy", title: "Excelencia e innovación", desc: "Soporte integral con tecnología, diseño y un enfoque 100% a resultados." },
];
---

<section id="trayectoria" class="py-20 px-4 bg-[var(--color-light)]">
  <div class="max-w-6xl mx-auto">
    <!-- Header -->
    <div class="text-center mb-16" data-aos="fade-up">
      <h2 class="text-4xl md:text-5xl font-bold text-[var(--color-primary)] mb-4">
        Nuestra <span class="text-[var(--color-secondary)]">Trayectoria</span>
      </h2>
      <p class="text-lg text-[color:color-mix(in_oklab,var(--color-dark)70%,var(--color-gray)30%)]">
        Más de tres décadas construyendo confianza y excelencia.
      </p>
    </div>

    <!-- Timeline -->
    <div class="relative">
      <!-- Línea vertical -->
      <div
        class="absolute top-0 bottom-0 w-[3px] bg-gradient-to-b from-[var(--color-secondary)] to-[var(--color-primary)]
               left-4 md:left-1/2 md:-translate-x-1/2"
        aria-hidden="true"
      ></div>

      <ol class="space-y-16" aria-label="Línea de tiempo de la empresa">
        {timeline.map((item, idx) => (
          <li
            class={`relative md:flex md:items-center md:gap-8 ${idx % 2 === 0 ? "md:flex-row" : "md:flex-row-reverse"}`}
            data-aos="fade-up"
            data-aos-delay={idx * 100}
          >
            <!-- Punto central -->
            <div
              class="absolute left-4 md:left-1/2 md:-translate-x-1/2 top-6 h-4 w-4 bg-[var(--color-secondary)] rounded-full shadow-md ring-4 ring-[var(--color-light)]"
              aria-hidden="true"
            ></div>

            <!-- Tarjeta -->
            <div
              class={`relative bg-white border border-[color:color-mix(in_oklab,var(--color-gray)70%,white_30%)]
                      rounded-xl p-6 shadow-md w-full md:w-[48%]
                      ${idx % 2 === 0 ? "md:text-right md:pr-8" : "md:text-left md:pl-8"}
                      hover:shadow-lg transition-all duration-300`}
            >
              <h3 class="text-2xl font-bold text-[var(--color-primary)] mb-1">{item.title}</h3>
              <p class="text-[var(--color-secondary)] font-semibold">{item.year}</p>
              <p class="mt-2 text-[color:color-mix(in_oklab,var(--color-dark)80%,var(--color-gray)20%)]">
                {item.desc}
              </p>
            </div>

            <!-- Espaciador para mantener estructura -->
            <div class="hidden md:block w-[48%]"></div>
          </li>
        ))}
      </ol>
    </div>
  </div>
</section>
